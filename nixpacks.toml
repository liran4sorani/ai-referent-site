[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = ["npm install -g pnpm@10.4.1", "pnpm install --frozen-lockfile"]

[phases.build]
cmds = ["echo 'CDN-FIX-BUILD-2026-06-15' && pnpm run build"]

[start]
cmd = "node dist/index.js"
